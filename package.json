{
  "name": "cycles-docs",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "convert-spec": "node scripts/convert-spec.js",
    "build": "npm run convert-spec && vitepress build",
    "dev": "npm run convert-spec && vitepress dev"
  },
  "dependencies": {
    "shiki": "^4.0.2",
    "vitepress-openapi": "^0.1.17",
    "yaml": "^2.8.2"
  }
}
